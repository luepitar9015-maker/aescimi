/**
 * auto_assign_by_permissions.js
 * 
 * Las asignaciones de expedientes se gestionan de forma manual y precisa a través del
 * módulo de importación/asignación por lotes de Excel o por asignación directa en el sistema.
 * 
 * Este script NO sobrescribe la tabla expediente_assignments.
 */

async function main() {
    console.log("=== AUTO-ASIGNACIÓN AUTOMÁTICA DESHABILITADA (USANDO ASIGNACIONES EXACTAS EN BD) ===");
}

main();
