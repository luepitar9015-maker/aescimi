# Este workflow fue DESACTIVADO.
# El SSL/HTTPS ahora es manejado completamente por NPM (Nginx Proxy Manager).
# Node.js corre HTTP puro en el puerto 80.
# NPM se encarga de los certificados Let's Encrypt y la terminación SSL.
#
# Para renovar certificados, use la interfaz web de NPM.
